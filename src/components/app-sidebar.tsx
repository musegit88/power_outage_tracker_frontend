import { Sidebar, SidebarHeader } from "@/components/ui/sidebar";

const AppSidebar = () => {
  return (
    <Sidebar>
      <SidebarHeader>
        <h1>Sidebar</h1>
      </SidebarHeader>
    </Sidebar>
  );
};

export default AppSidebar;
